"use client";

export default function Carrousel() {
  const settings = {
    dots: true,
    infinite: true,
    speed: 500,
    slidesToShow: 1,
    slidesToScroll: 1,
  };
  return (
    <div className="carrousel-container">
      {/* Carrousel implementation goes here */}
    </div>
  );
}
